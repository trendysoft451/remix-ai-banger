{
  "name": "remix-banger-gpt-action-render",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Passerelle Render/GitHub pour exposer startProfessionalRemix à une Action GPT.",
  "engines": {
    "node": ">=20 <25"
  },
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "check": "node --check server.mjs && node --check scripts/generate-openapi.mjs",
    "test": "node --test",
    "openapi": "node scripts/generate-openapi.mjs"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "zod": "^3.24.2"
  }
}
